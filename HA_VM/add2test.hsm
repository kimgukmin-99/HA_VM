// A + B
int8 add2(int8 A, int8 B) {
	int8 A = 0, B = 0;
	A = pop(); // argc ¹ö¸²
	B = pop();
	A = pop();
	B += A;
	return B;
}
!
alias A cnt
alias B x
alias C sum
alias D zero
pop cnt
push 50
push 12
pop sum
add sum 3
sub sum 3
neg sum
acc sum cnt
ret